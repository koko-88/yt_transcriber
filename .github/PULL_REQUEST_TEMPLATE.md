## What changed

<!-- One or two sentences on the user-visible effect. -->

## Why

<!-- The problem, defect or requirement this addresses. -->

## Verification

<!-- Commands run (e.g. `npm run verify`) and manual checks performed. -->

## Checklist

- [ ] `npm run verify` passes locally
- [ ] New behaviour has tests (unit or E2E) and bug fixes have a regression test
- [ ] No new permission, host permission, remote code or user-data collection — or it is justified above and added to `SECURITY.md`
- [ ] User-facing strings added to both `en` and `ar` catalogs in `src/core/i18n.ts`
- [ ] Documentation updated when behaviour or configuration changed
